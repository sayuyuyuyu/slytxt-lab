---
import { pagePath, visiblePages } from "@/lib/pagination";

type Props = {
  basePath: string;
  currentPage: number;
  totalPages: number;
  prevUrl?: string;
  nextUrl?: string;
};

const { basePath, currentPage, totalPages, prevUrl, nextUrl } = Astro.props;
---

{
  totalPages > 1 && (
    <nav class="pagination" aria-label="ページネーション" data-pagefind-ignore>
      {prevUrl ? <a href={prevUrl}>前へ</a> : <span aria-disabled="true">前へ</span>}
      {visiblePages(currentPage, totalPages).map((page) => {
        if (page === "gap") return <span class="pagination__gap" aria-hidden="true">…</span>;
        const href = pagePath(basePath, page);
        return page === currentPage ? (
          <span aria-current="page">{page}</span>
        ) : (
          <a href={href} aria-label={`${page}ページ目`}>{page}</a>
        );
      })}
      {nextUrl ? <a href={nextUrl}>次へ</a> : <span aria-disabled="true">次へ</span>}
    </nav>
  )
}
