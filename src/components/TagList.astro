---
import { tagPath } from "@/lib/tags";

type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <ul class="tag-list" aria-label="タグ">
      {tags.map((tag) => (
        <li>
          <a class="tag" href={tagPath(tag)}>
            <span class="tag__hash" aria-hidden="true">#</span>{tag}
          </a>
        </li>
      ))}
    </ul>
  )
}
